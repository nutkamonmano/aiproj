{
  "name": "aiproj",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && echo '--- Build complete! Now commit and push the docs/ folder to gh-pages ---'"
  },
  "devDependencies": {
    "vite": "^8.0.10"
  },
  "dependencies": {
    "@imgly/background-removal": "^1.7.0"
  }
}
